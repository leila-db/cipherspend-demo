# Runtime dependencies for the CipherSpend local web app.
# The FHE work is done by the compiled OpenFHE Stage-5 binaries (see TESTING.md);
# these are the only Python packages the running app needs.
Flask>=3.0
numpy>=1.24

# Dev-only (NOT needed to run the app):
#   Running the test suite (python -m pytest -q) requires:
#     pytest>=7.0
#   Retraining the categorizer (python scripts/train_categorizer.py) requires:
#     scikit-learn>=1.3
#     scipy>=1.10
